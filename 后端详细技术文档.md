# 基于FunASR的AI智能会议纪要系统 - 后端详细技术文档

---

## 目录

1. [项目概述](#1-项目概述)
2. [目录结构](#2-目录结构)
3. [核心配置文件](#3-核心配置文件)
4. [数据模型层](#4-数据模型层)
5. [API视图层](#5-api视图层)
6. [工具模块](#6-工具模块)
7. [RAG模块](#7-rag模块)
8. [管理命令](#8-管理命令)
9. [数据库迁移](#9-数据库迁移)

---

## 1. 项目概述

### 1.1 项目定位

本项目是基于 **Django 6.0.3** 和 **FunASR** 构建的AI智能会议纪要系统后端服务，提供：
- 语音/视频转文字（ASR）
- 说话人识别与声纹匹配
- 会议纪要与摘要生成（基于LLM）
- 智能问答（RAG检索）
- 文件管理与导出

### 1.2 技术栈

| 分类 | 技术 | 版本 |
|------|------|------|
| 框架 | Django | 6.0.3 |
| API框架 | Django REST Framework | 3.x |
| 数据库 | SQLite3 | - |
| ASR引擎 | FunASR | 最新 |
| LLM | Qwen/Qwen3.5-35B-A3B | ModelScope |
| 文件导出 | python-docx | - |

---

## 2. 目录结构

```
asr-backend/asr_meeting_service/
├── asr_meeting_service/          # Django项目配置目录
│   ├── __init__.py
│   ├── asgi.py                   # ASGI服务器入口
│   ├── settings.py               # 全局配置
│   ├── urls.py                   # 根URL路由
│   └── wsgi.py                   # WSGI服务器入口
├── asr_api/                      # 核心应用模块
│   ├── __init__.py
│   ├── admin.py                  # Django Admin配置
│   ├── apps.py                   # 应用配置
│   ├── models.py                 # 数据模型
│   ├── urls.py                   # API路由
│   ├── auth_utils.py             # 认证工具
│   ├── utils.py                  # 通用工具
│   ├── voiceprint_utils.py       # 声纹处理工具
│   ├── indexer_manager.py        # RAG索引管理器
│   ├── management/               # 管理命令
│   │   ├── __init__.py
│   │   └── commands/
│   │       ├── __init__.py
│   │       └── init_prompt_templates.py
│   ├── migrations/               # 数据库迁移文件
│   ├── rag/                      # RAG模块
│   │   ├── __init__.py
│   │   ├── agent.py
│   │   ├── config.py
│   │   ├── embedding.py
│   │   ├── indexer.py
│   │   ├── retriever.py
│   │   └── vector_store.py
│   └── views/                    # API视图
│       ├── __init__.py
│       ├── base.py
│       ├── asr.py
│       ├── video.py
│       ├── meeting.py
│       ├── meeting_analysis.py
│       ├── export.py
│       ├── voiceprint.py
│       ├── speaker.py
│       ├── user.py
│       ├── file.py
│       ├── transcription.py
│       ├── upload_transcribe.py
│       ├── prompt.py
│       └── rag.py
├── manage.py                     # Django管理命令入口
├── requirements_rag.txt          # 依赖列表
├── clean_migrations.py           # 清理迁移脚本
├── migrate_old_data.py           # 数据迁移脚本
├── test.py                       # 测试脚本
├── test_model.py                 # 模型测试脚本
└── test_rag_offline.py           # RAG离线测试脚本
```

---

## 3. 核心配置文件

### 3.1 `asr_meeting_service/settings.py`

**作用**：Django项目的核心配置文件，包含数据库、中间件、安全等所有配置。

**关键配置项**：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `DEBUG` | `True` | 调试模式开关 |
| `SECRET_KEY` | `django-insecure-xxx` | 安全密钥 |
| `ALLOWED_HOSTS` | `[]` | 允许的主机列表 |
| `DATABASES` | SQLite | 数据库配置 |
| `INSTALLED_APPS` | rest_framework, corsheaders, asr_api | 注册的应用 |
| `MIDDLEWARE` | 包含CORS中间件 | 中间件配置 |
| `REST_FRAMEWORK` | Token认证配置 | REST框架配置 |
| `MEDIA_ROOT` | `media/` | 媒体文件目录 |
| `FILE_UPLOAD_DIR` | `uploaded_files/` | 上传文件目录 |
| `VOICEPRINT_DIR` | `voiceprints/` | 声纹文件目录 |
| `LLM_CONFIG` | ModelScope配置 | LLM服务配置 |

### 3.2 `asr_meeting_service/urls.py`

**作用**：根URL配置，将请求路由到`asr_api`应用。

```python
urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/', include('asr_api.urls')),
]
```

### 3.3 `asr_api/urls.py`

**作用**：API路由配置，定义所有API端点。

**路由分类**：
- 基础接口：健康检查
- ASR接口：语音/视频转文字
- LLM接口：会议纪要、摘要、分段
- 导出接口：Word文档导出
- 声纹接口：声纹管理
- Speaker接口：说话人管理
- 用户接口：用户认证与管理
- 文件接口：文件上传下载
- 逐字稿接口：转录文本管理
- Prompt接口：模板管理
- RAG接口：智能问答

---

## 4. 数据模型层

### 4.1 `asr_api/models.py`

**作用**：定义数据库模型和FunASR模型加载逻辑。

**模型列表**：

| 模型名 | 说明 | 核心字段 |
|--------|------|----------|
| `User` | 用户模型 | username, email, avatar |
| `AudioRecord` | 音频记录 | filename, transcription, summary |
| `Speaker` | 说话人主表 | name, user, avatar |
| `Voiceprint` | 声纹子表 | speaker, feature, source_type |
| `UploadedFile` | 上传文件 | user, original_name, file_path, file_type |
| `Transcription` | 转录记录 | file, transcription_text, segments |
| `Prompt` | Prompt模板 | name, system_prompt, user_prompt |
| `MeetingSummary` | 会议纪要 | file, summary_text, abstract_text |
| `MeetingSegment` | 会议分段 | file, segment_index, start_time, end_time, title, content, summary |

**FunASR模型加载**：
- `load_asr_model()`：加载语音识别模型（Paraformer-zh + VAD + PUNC + CAM++）
- `load_voiceprint_model()`：加载纯声纹模型（Speech_Campplus_SV）
- `ready()`：Django启动时自动加载模型

---

## 5. API视图层

### 5.1 `views/__init__.py`

**作用**：导出所有视图类，保持引用一致性。

### 5.2 `views/base.py`

**作用**：基础接口视图。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `HealthCheckView` | GET | `/` | 健康检查接口 |

### 5.3 `views/user.py`

**作用**：用户管理接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `UserRegisterView` | POST | `/user/register` | 用户注册 |
| `UserLoginView` | POST | `/user/login` | 用户登录 |
| `UserLogoutView` | POST | `/user/logout` | 用户退出 |
| `UserProfileView` | GET/PUT | `/user/profile` | 获取/更新用户信息 |
| `UserAvatarUploadView` | POST | `/user/avatar/upload` | 头像上传 |
| `UserAvatarDeleteView` | DELETE | `/user/avatar/delete` | 头像删除 |

### 5.4 `views/file.py`

**作用**：文件管理接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `FileUploadView` | POST | `/file/upload` | 文件上传 |
| `FileListView` | GET | `/file/list` | 文件列表（支持搜索） |
| `FileRenameView` | POST | `/file/rename` | 文件重命名 |
| `FileDownloadView` | GET | `/file/download/<file_id>` | 文件下载（支持Range请求） |
| `FileDeleteView` | POST | `/file/delete` | 文件删除 |

### 5.5 `views/asr.py`

**作用**：音频ASR语音转文字接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `ASRTranscribeView` | POST | `/asr` | 音频语音转文字（含说话人分离和声纹匹配） |

**核心流程**：
1. 接收音频文件
2. 调用FunASR进行说话人分离和语音识别
3. 对每个说话人提取声纹特征
4. 匹配用户已注册的声纹
5. 返回结构化的分段结果

### 5.6 `views/video.py`

**作用**：视频ASR接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `VideoASRTranscribeView` | POST | `/video_asr` | 视频语音转文字（先提取音频） |

### 5.7 `views/meeting.py`

**作用**：会议纪要和摘要相关接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `MeetingSummaryView` | POST | `/generate_summary` | 生成会议纪要 |
| `MeetingAbstractView` | POST | `/meeting_abstract` | 生成会议摘要 |
| `GetMeetingSummaryView` | GET | `/meeting/summary/get` | 获取会议纪要 |
| `GetMeetingAbstractView` | GET | `/meeting/abstract/get` | 获取会议摘要 |
| `MeetingSummaryUpdateView` | PUT | `/meeting/summary/update` | 更新会议纪要 |
| `MeetingAbstractUpdateView` | PUT | `/meeting/abstract/update` | 更新会议摘要 |
| `GenerateSegmentsView` | POST | `/meeting/segments/generate` | 生成分段 |
| `SegmentsListView` | GET | `/meeting/segments/list` | 获取分段列表 |
| `SegmentUpdateView` | POST | `/meeting/segments/update/<id>` | 更新分段 |

### 5.8 `views/transcription.py`

**作用**：逐字稿管理接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `TranscriptionSearchView` | POST | `/transcription/search` | 逐字稿搜索 |
| `TranscriptionGetView` | GET | `/transcription/get/<file_id>` | 获取逐字稿 |
| `TranscriptionEditView` | PUT | `/transcription/edit` | 编辑逐字稿 |
| `TranscriptionGenerateView` | POST | `/transcription/generate` | 生成逐字稿 |

### 5.9 `views/voiceprint.py`

**作用**：声纹管理接口（旧版兼容）。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `VoiceprintAddView` | POST | `/voiceprint/add` | 添加声纹 |
| `VoiceprintListView` | GET | `/voiceprint/list` | 获取声纹列表 |
| `VoiceprintRenameView` | POST | `/voiceprint/rename` | 重命名声纹 |
| `VoiceprintDeleteView` | DELETE | `/voiceprint/delete` | 删除声纹 |
| `VoiceprintAudioView` | GET | `/voiceprint/audio/<vp_id>` | 获取声纹音频 |
| `VoiceprintAvatarUploadView` | POST | `/voiceprint/avatar/upload/<vp_id>` | 上传声纹头像 |
| `VoiceprintAvatarDeleteView` | DELETE | `/voiceprint/avatar/delete/<vp_id>` | 删除声纹头像 |

### 5.10 `views/speaker.py`

**作用**：说话人管理接口（新版）。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `SpeakerListView` | GET | `/speaker/list` | 获取说话人列表 |
| `SpeakerAddView` | POST | `/speaker/add` | 创建说话人 |
| `SpeakerUpdateView` | POST | `/speaker/update` | 更新说话人 |
| `SpeakerDeleteView` | DELETE | `/speaker/delete` | 删除说话人 |
| `SpeakerAvatarUploadView` | POST | `/speaker/avatar/upload/<sp_id>` | 上传说话人头像 |
| `SpeakerAvatarDeleteView` | DELETE | `/speaker/avatar/delete/<sp_id>` | 删除说话人头像 |
| `SpeakerVoiceprintAddView` | POST | `/speaker/<sp_id>/voiceprint/add` | 追加声纹 |
| `SpeakerVoiceprintDeleteView` | DELETE | `/speaker/<sp_id>/voiceprint/delete/<vp_id>` | 删除声纹 |
| `SpeakerVoiceprintUpdateView` | POST | `/speaker/<sp_id>/voiceprint/update/<vp_id>` | 更新声纹 |
| `SpeakerVoiceprintAudioView` | GET | `/speaker/<sp_id>/voiceprint/audio/<vp_id>` | 获取声纹音频 |

### 5.11 `views/prompt.py`

**作用**：Prompt模板管理接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `PromptListView` | GET | `/prompt/list` | 获取模板列表 |
| `PromptAddView` | POST | `/prompt/add` | 添加模板 |
| `PromptUpdateView` | POST | `/prompt/update/<prompt_id>` | 更新模板 |
| `PromptDeleteView` | DELETE | `/prompt/delete/<prompt_id>` | 删除模板 |
| `PromptCopyView` | GET | `/prompt/copy/<prompt_id>` | 获取模板内容（用于复制） |

### 5.12 `views/export.py`

**作用**：Word文档导出接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `ExportTranscriptionWordView` | POST | `/export_transcription_word` | 导出转录文本 |
| `ExportSummaryWordView` | POST | `/export_summary_word` | 导出会议纪要 |
| `ExportAbstractWordView` | POST | `/export_abstract_word` | 导出会议摘要 |

### 5.13 `views/meeting_analysis.py`

**作用**：会议分析接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `MeetingAnalysisView` | GET | `/meeting/analysis/<file_id>` | 获取会议分析报告 |

### 5.14 `views/upload_transcribe.py`

**作用**：文件上传并转录接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `FileUploadTranscribeView` | POST | `/file/upload_transcribe` | 上传文件并自动转录 |

### 5.15 `views/rag.py`

**作用**：RAG智能问答接口。

| 视图类 | 方法 | 路径 | 说明 |
|--------|------|------|------|
| `ChatView` | POST | `/rag/chat` | RAG聊天 |
| `IndexFileView` | POST | `/rag/index` | 索引文件 |
| `IndexStatusView` | GET | `/rag/status` | 索引状态 |
| `TestRetrieveView` | POST | `/rag/test` | 测试检索 |

---

## 6. 工具模块

### 6.1 `asr_api/auth_utils.py`

**作用**：认证相关工具函数。

| 函数 | 说明 |
|------|------|
| `create_token(user)` | 创建或获取用户Token |
| `authenticate_user(username, password)` | 验证用户并返回Token |
| `get_user_from_token(token)` | 从Token获取用户 |
| `require_auth(view_func)` | 认证装饰器 |

### 6.2 `asr_api/utils.py`

**作用**：通用工具函数。

| 函数 | 说明 |
|------|------|
| `custom_exception_handler(exc, context)` | 自定义异常处理 |
| `extract_audio_from_video(video_path, output_audio_path)` | 从视频提取音频 |

### 6.3 `asr_api/voiceprint_utils.py`

**作用**：声纹处理专用工具函数。

| 函数 | 说明 |
|------|------|
| `extract_voiceprint_feature(audio_path)` | 提取声纹特征 |
| `check_voiceprint_duplicate(feature, user)` | 检查声纹是否重复 |
| `match_voiceprint(feature, user, speaker_count)` | 匹配声纹 |
| `append_voiceprint(speaker, feature, ...)` | 追加声纹 |
| `match_voiceprint_old(feature, user, speaker_count)` | 旧版声纹匹配（兼容） |

### 6.4 `asr_api/indexer_manager.py`

**作用**：RAG索引任务管理器。

| 方法 | 说明 |
|------|------|
| `get_status(file_id)` | 获取文件索引状态 |
| `is_indexing(file_id)` | 检查是否正在索引 |
| `start_indexing(file_id)` | 开始索引 |
| `update_progress(file_id, progress)` | 更新索引进度 |
| `finish_indexing(file_id, success, error_msg)` | 完成索引 |
| `reset_status(file_id)` | 重置索引状态 |
| `get_all_status()` | 获取所有索引状态 |

---

## 7. RAG模块

### 7.1 `rag/config.py`

**作用**：RAG配置管理。

### 7.2 `rag/embedding.py`

**作用**：文本嵌入（Embedding）处理。

### 7.3 `rag/vector_store.py`

**作用**：向量存储管理。

### 7.4 `rag/indexer.py`

**作用**：文档索引器。

### 7.5 `rag/retriever.py`

**作用**：文档检索器。

### 7.6 `rag/agent.py`

**作用**：RAG对话代理。

---

## 8. 管理命令

### 8.1 `management/commands/init_prompt_templates.py`

**作用**：初始化Prompt模板数据。

**使用方式**：
```bash
python manage.py init_prompt_templates
```

---

## 9. 数据库迁移

### 9.1 迁移文件列表

| 迁移文件 | 说明 |
|----------|------|
| `0001_initial.py` | 初始迁移（User, AudioRecord等） |
| `0002_voiceprint_user_alter_voiceprint_name_and_more.py` | 声纹模型更新 |
| `0003_voiceprint_file_path.py` | 添加声纹文件路径字段 |
| `0004_prompt.py` | 添加Prompt模型 |
| `0005_prompt_template_type.py` | 添加模板类型字段 |
| `0006_meetingsegment.py` | 添加会议分段模型 |
| `0007_meetingsummarysection.py` | 添加会议摘要部分 |
| `0008_add_user_avatar.py` | 添加用户头像字段 |
| `0009_voiceprint_avatar.py` | 添加声纹头像字段 |
| `0010_transcription_raw_sentence_info_and_more.py` | 转录模型更新 |
| `0011_alter_voiceprint_options_and_more.py` | 声纹选项更新 |
| `0012_add_transcription_segments_field.py` | 添加转录分段字段 |
| `0013_remove_transcription_raw_sentence_info_and_more.py` | 移除旧字段 |

### 9.2 迁移命令

```bash
# 创建迁移文件
python manage.py makemigrations

# 执行迁移
python manage.py migrate

# 查看迁移状态
python manage.py showmigrations
```

---

## 附录：文件功能速查表

| 文件路径 | 功能描述 |
|----------|----------|
| `manage.py` | Django管理命令入口 |
| `requirements_rag.txt` | 项目依赖列表 |
| `clean_migrations.py` | 清理迁移脚本 |
| `migrate_old_data.py` | 旧数据迁移脚本 |
| `test.py` | 通用测试脚本 |
| `test_model.py` | 模型测试脚本 |
| `test_rag_offline.py` | RAG离线测试脚本 |
